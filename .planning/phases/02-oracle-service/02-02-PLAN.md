---
phase: 02-oracle-service
plan: 02
type: execute
wave: 2
depends_on: [02-01]
files_modified:
  - oracle/package.json
  - oracle/tsconfig.json
  - oracle/src/config.ts
  - oracle/src/types.ts
  - oracle/src/logger.ts
  - oracle/src/chain.ts
  - oracle/config/system-prompt.txt
autonomous: true
requirements: [ORC-02, ORC-05]

must_haves:
  truths:
    - "Oracle can load the Anchor IDL and derive all PDAs needed for chain interactions"
    - "Oracle can call open_round, lock_round, resolve_round, resolve_round_vrf, and set_arweave_txid via the chain client"
    - "Oracle can read SeasonState and PixelState accounts from chain"
    - "All environment variables are validated at startup — missing vars cause immediate exit with clear error"
  artifacts:
    - path: "oracle/package.json"
      provides: "Node.js project config with all dependencies"
      contains: "@anthropic-ai/sdk"
    - path: "oracle/tsconfig.json"
      provides: "TypeScript configuration"
      contains: "compilerOptions"
    - path: "oracle/src/config.ts"
      provides: "Env var loading and validation"
      exports: ["loadConfig", "OracleConfig"]
      min_lines: 40
    - path: "oracle/src/types.ts"
      provides: "Shared type definitions"
      exports: ["ClaudeResult", "RoundHistoryEntry", "COLOR_NAMES"]
      min_lines: 30
    - path: "oracle/src/logger.ts"
      provides: "Structured JSON logger"
      exports: ["logger"]
      min_lines: 15
    - path: "oracle/src/chain.ts"
      provides: "Anchor program client with all oracle instructions"
      exports: ["ChainClient"]
      min_lines: 100
    - path: "oracle/config/system-prompt.txt"
      provides: "AI system prompt template loaded at runtime"
      contains: "abstract artist"
  key_links:
    - from: "oracle/src/chain.ts"
      to: "target/idl/pixel_predict.json"
      via: "IDL import for Anchor program client"
      pattern: "pixel_predict"
    - from: "oracle/src/config.ts"
      to: "oracle/src/chain.ts"
      via: "OracleConfig provides RPC URL, keypair, program ID"
      pattern: "OracleConfig"
---

<objective>
Scaffold the oracle Node.js project and build the chain client module that wraps all Solana/Anchor interactions — the foundation layer that every other oracle module depends on.

Purpose: Every subsequent plan needs the project structure, type definitions, and chain client to exist. By building this first with thorough type exports and a working Anchor client, all downstream plans (prompt, Claude, lifecycle, Arweave) have stable contracts to implement against.

Output: Working `oracle/` subdirectory with package.json, TypeScript config, env validation, structured logger, shared types, chain client module, and system prompt template file.
</objective>

<execution_context>
@/Users/puranjaysingh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/puranjaysingh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-oracle-service/02-CONTEXT.md
@.planning/phases/02-oracle-service/02-RESEARCH.md
@.planning/phases/02-oracle-service/02-01-SUMMARY.md

<interfaces>
<!-- From updated IDL (after Plan 01) — instructions the chain client must wrap -->

Instructions the oracle calls:
- open_round(pixel_index: u16, prompt_hash: [u8; 32]) — accounts: season_state, pixel_state, config, oracle (signer), system_program
- lock_round(season_number: u16, pixel_index: u16) — accounts: pixel_state, caller (signer)
- resolve_round(winning_color: u8, shade: u8, warmth: u8, vrf_resolved: bool) — accounts: season_state, pixel_state, config, oracle (signer), treasury, jackpot, system_program
- resolve_round_vrf() — accounts: season_state, pixel_state, config, oracle (signer), treasury, jackpot, randomness_account, system_program
- set_arweave_txid(season_number: u16, pixel_index: u16, arweave_txid: [u8; 43]) — accounts: pixel_state, config, oracle (signer)

Accounts the oracle reads:
- SeasonState: season_number, grid_width, grid_height, current_pixel_index, status, total_volume, total_bets
- PixelState: season_number, pixel_index, x, y, status, color_pools, winning_color, shade, warmth, prompt_hash, arweave_txid, has_arweave_txid, vrf_resolved, opened_at, locked_at, resolved_at

PDA seeds:
- config: ["config"]
- season: ["season", season_number.to_le_bytes()]
- pixel: ["pixel", season_number.to_le_bytes(), pixel_index.to_le_bytes()]

Program ID: FaVo1qzkbVt1uPwyU4jRZ7hgkJbYTzat8iqtPE3orxQG
Treasury: 6vTe3xRjB4Hv4fN4WQ5xtcF21Ed12DFPoNwHJTZDUg5v
Jackpot: HrfnbCNRzvekRkdUJGzvmEu478F43uk7weReNDPqv2TB
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Scaffold oracle project with config, types, and logger</name>
  <files>
    oracle/package.json,
    oracle/tsconfig.json,
    oracle/.env.example,
    oracle/src/config.ts,
    oracle/src/types.ts,
    oracle/src/logger.ts,
    oracle/config/system-prompt.txt,
    .gitignore
  </files>
  <action>
    Create `oracle/` directory at project root as a self-contained Node.js TypeScript project.

    **oracle/package.json:**
    - name: "pixel-predict-oracle"
    - scripts: `"build": "tsc"`, `"start": "node dist/index.js"`, `"dev": "tsx src/index.ts"`, `"test": "ts-mocha -p ./tsconfig.json -t 10000 src/__tests__/**/*.ts"`
    - dependencies: `@anthropic-ai/sdk`, `@coral-xyz/anchor`, `@solana/web3.js`, `node-cron`, `node-telegram-bot-api`, `dotenv`
    - devDependencies: `typescript`, `tsx`, `@types/node`, `@types/node-cron`, `@types/node-telegram-bot-api`, `ts-mocha`, `mocha`, `chai`, `@types/mocha`, `@types/chai`
    - Note: `@irys/upload-solana` and `@switchboard-xyz/on-demand` will be added in later plans when their exact APIs are verified during implementation

    **oracle/tsconfig.json:**
    - target: ES2022, module: commonjs, moduleResolution: node, outDir: dist, rootDir: src
    - strict: true, esModuleInterop: true, resolveJsonModule: true, skipLibCheck: true, declaration: true
    - include: ["src/**/*"], exclude: ["node_modules", "dist"]

    **oracle/.env.example:**
    - List all env vars from SPEC: SOLANA_RPC_URL, ORACLE_KEYPAIR, ANTHROPIC_API_KEY, PROGRAM_ID, TREASURY_WALLET, JACKPOT_WALLET, CURRENT_SEASON, SEASON_STYLE_SUMMARY, TELEGRAM_BOT_TOKEN, TELEGRAM_CHAT_ID

    **oracle/src/config.ts:**
    - Export `OracleConfig` interface with all env vars typed
    - Export `loadConfig(): OracleConfig` function:
      - Call `dotenv.config()` for local dev
      - Validate all required env vars are present (SOLANA_RPC_URL, ORACLE_KEYPAIR, ANTHROPIC_API_KEY, PROGRAM_ID, CURRENT_SEASON) — exit process with clear error message listing missing vars
      - Parse ORACLE_KEYPAIR: `Keypair.fromSecretKey(Uint8Array.from(JSON.parse(process.env.ORACLE_KEYPAIR!.trim())))`
      - Validate keypair loads correctly (wrap in try/catch with descriptive error)
      - SEASON_STYLE_SUMMARY defaults to empty string for Season 1
      - TELEGRAM_BOT_TOKEN and TELEGRAM_CHAT_ID are optional (alerts disabled if missing, log warning)
      - Return typed config object

    **oracle/src/types.ts:**
    - Export `COLOR_NAMES` array: ['Red','Orange','Yellow','Lime','Green','Teal','Cyan','Blue','Indigo','Purple','Pink','Magenta','Brown','Gray','Black','White']
    - Export `ClaudeResult` interface: { colorIndex: number, colorName: string, shade: number, warmth: number, reasoning: string }
    - Export `RoundHistoryEntry` interface: { pixelIndex: number, x: number, y: number, color: string, shade: number, warmth: number, reasoning: string }
    - Export `PixelData` interface: { x: number, y: number, color: string, shade: number, warmth: number }
    - Export `ResolutionResult` type: ClaudeResult & { vrfResolved: false } | { colorIndex: number, shade: 50, warmth: 50, vrfResolved: true }

    **oracle/src/logger.ts:**
    - Export `logger` object with methods: info, warn, error, debug
    - Each method accepts an object and writes JSON.stringify to stdout/stderr
    - Add `timestamp` field (ISO string) and `level` field to every log entry
    - Keep it minimal — structured JSON logging for Railway log search

    **oracle/config/system-prompt.txt:**
    - Copy the exact system prompt template from SPEC.md (the one with {grid_width}, {grid_height}, {season_style_summary} placeholders)
    - Use exact text: "You are an abstract artist creating a {grid_width}x{grid_height} canvas..." through "REASONING: [1-2 sentences about your choice]"

    **Update .gitignore** (project root): add `oracle/node_modules/`, `oracle/dist/`, `oracle/round_history.json`, `oracle/.env`

    Run `cd oracle && npm install` to install all dependencies.
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/oracle && npm install && npx tsc --noEmit</automated>
  </verify>
  <done>oracle/ directory exists with package.json, tsconfig.json, config.ts exports loadConfig/OracleConfig, types.ts exports COLOR_NAMES/ClaudeResult/RoundHistoryEntry, logger.ts exports structured JSON logger, system-prompt.txt contains the SPEC template, `tsc --noEmit` passes with no type errors</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build chain client module wrapping all Anchor interactions</name>
  <files>
    oracle/src/chain.ts,
    oracle/src/__tests__/chain.test.ts
  </files>
  <behavior>
    - Test 1: ChainClient.deriveConfigPDA returns expected PDA for program ID
    - Test 2: ChainClient.deriveSeasonPDA returns expected PDA for season 1
    - Test 3: ChainClient.derivePixelPDA returns expected PDA for season 1, pixel 0
    - Test 4: ChainClient constructor throws if IDL file not found at expected path
  </behavior>
  <action>
    Create `oracle/src/chain.ts`:
    - Export `ChainClient` class:
      - Constructor: accepts `OracleConfig`, creates `Connection`, loads IDL from `../target/idl/pixel_predict.json` (path relative to oracle/), creates `AnchorProvider` with oracle wallet, creates `Program<PixelPredict>` instance
      - PDA derivation methods (static): `deriveConfigPDA()`, `deriveSeasonPDA(seasonNumber)`, `derivePixelPDA(seasonNumber, pixelIndex)` — use the same seed patterns from tests/helpers.ts (Buffer.alloc(2).writeUInt16LE)
      - `getSeasonState(seasonNumber)`: fetch SeasonState account, return typed object with current_pixel_index, status, grid_width, grid_height
      - `getPixelState(seasonNumber, pixelIndex)`: fetch PixelState account, return typed object or null if not found
      - `getAllResolvedPixels(seasonNumber, maxIndex)`: iterate pixel indices 0..maxIndex, fetch each PixelState, filter to Resolved status, return array of PixelData with color name from COLOR_NAMES
      - `openRound(pixelIndex, promptHash)`: call program.methods.openRound with correct accounts
      - `lockRound(seasonNumber, pixelIndex)`: call program.methods.lockRound with correct accounts
      - `resolveRound(seasonNumber, pixelIndex, result: ClaudeResult)`: call program.methods.resolveRound with winning_color, shade, warmth, vrf_resolved=false
      - `resolveRoundVrf(seasonNumber, pixelIndex, randomnessAccount: PublicKey)`: call program.methods.resolveRoundVrf with correct accounts
      - `setArweaveTxid(seasonNumber, pixelIndex, txid: string)`: encode 43-char string to [u8; 43], call program.methods.setArweaveTxid
      - Each method includes `try/catch` with structured logging on failure, re-throws with descriptive message

    Create `oracle/src/__tests__/chain.test.ts`:
    - Test PDA derivation methods match expected outputs (compare against known PDAs from tests/helpers.ts patterns)
    - Use mocha/chai — these are pure unit tests, no Solana connection needed for PDA derivation

    Note on IDL path: The chain client will load the IDL at `../target/idl/pixel_predict.json` relative to the oracle/ directory. The IDL was updated in Plan 01 to include set_arweave_txid. Import the IDL type from `../../target/types/pixel_predict` for TypeScript type safety.
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/oracle && npx ts-mocha -p ./tsconfig.json -t 10000 src/__tests__/chain.test.ts</automated>
  </verify>
  <done>ChainClient class exports with all oracle instruction methods, PDA derivation tests pass, TypeScript compiles with no errors</done>
</task>

</tasks>

<verification>
- `cd oracle && npx tsc --noEmit` passes with zero type errors
- `cd oracle && npx ts-mocha -p ./tsconfig.json -t 10000 src/__tests__/chain.test.ts` passes
- oracle/src/config.ts, oracle/src/types.ts, oracle/src/logger.ts, oracle/src/chain.ts all exist with proper exports
- oracle/config/system-prompt.txt contains the SPEC prompt template
</verification>

<success_criteria>
A functional oracle/ subdirectory with all infrastructure code (config, types, logger) and a complete chain client module that wraps every Anchor instruction the oracle needs. All downstream plans can import from these modules without modifications.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-service/02-02-SUMMARY.md`
</output>
