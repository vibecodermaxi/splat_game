---
phase: 02-oracle-service
plan: 03
type: execute
wave: 3
depends_on: [02-02]
files_modified:
  - oracle/src/prompt.ts
  - oracle/src/claude.ts
  - oracle/src/__tests__/prompt.test.ts
  - oracle/src/__tests__/claude.test.ts
autonomous: true
requirements: [ORC-01, ORC-03, ORC-04, ORC-11]

must_haves:
  truths:
    - "Oracle constructs a prompt from canvas state, neighbors, and last 5 selections that matches the SPEC format"
    - "Oracle calls Claude at temperature 0 and parses COLOR, SHADE, WARMTH, REASONING from the response"
    - "Oracle rejects AI responses where color is not one of 16, shade is not 0-100, or warmth is not 0-100"
    - "Season style summary is injected into system prompt when SEASON_STYLE_SUMMARY env var is set"
  artifacts:
    - path: "oracle/src/prompt.ts"
      provides: "Prompt construction from canvas state and round history"
      exports: ["buildSystemPrompt", "buildUserMessage", "buildFullPrompt", "hashPrompt"]
      min_lines: 60
    - path: "oracle/src/claude.ts"
      provides: "Claude API client with response parsing and validation"
      exports: ["callClaude", "parseClaudeResponse", "validateClaudeResult"]
      min_lines: 50
    - path: "oracle/src/__tests__/prompt.test.ts"
      provides: "Unit tests for prompt construction"
      min_lines: 40
    - path: "oracle/src/__tests__/claude.test.ts"
      provides: "Unit tests for Claude response parsing and validation"
      min_lines: 50
  key_links:
    - from: "oracle/src/prompt.ts"
      to: "oracle/config/system-prompt.txt"
      via: "fs.readFileSync at startup"
      pattern: "system-prompt\\.txt"
    - from: "oracle/src/prompt.ts"
      to: "oracle/src/types.ts"
      via: "PixelData, RoundHistoryEntry types"
      pattern: "PixelData|RoundHistoryEntry"
    - from: "oracle/src/claude.ts"
      to: "@anthropic-ai/sdk"
      via: "messages.create API call"
      pattern: "messages\\.create"
---

<objective>
Build the prompt construction module (ORC-01, ORC-11) and Claude API client module (ORC-03, ORC-04) with thorough unit tests. These are pure logic modules with well-defined I/O — ideal for test-first development.

Purpose: The prompt is the soul of the game. It must exactly match the SPEC format, produce a stable SHA-256 hash for commit-reveal, and handle edge cases (empty canvas, no history, season style summary). The Claude client must parse structured responses reliably and reject invalid data before it reaches the chain.

Output: `prompt.ts` with prompt construction + SHA-256 hashing, `claude.ts` with API call + response parsing + validation, both with comprehensive unit tests.
</objective>

<execution_context>
@/Users/puranjaysingh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/puranjaysingh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/02-oracle-service/02-CONTEXT.md
@.planning/phases/02-oracle-service/02-RESEARCH.md
@.planning/phases/02-oracle-service/02-02-SUMMARY.md

<interfaces>
<!-- Types from oracle/src/types.ts (created in Plan 02) -->
```typescript
export const COLOR_NAMES = ['Red','Orange','Yellow','Lime','Green','Teal','Cyan','Blue',
                            'Indigo','Purple','Pink','Magenta','Brown','Gray','Black','White'];

export interface ClaudeResult {
  colorIndex: number;
  colorName: string;
  shade: number;
  warmth: number;
  reasoning: string;
}

export interface RoundHistoryEntry {
  pixelIndex: number;
  x: number;
  y: number;
  color: string;
  shade: number;
  warmth: number;
  reasoning: string;
}

export interface PixelData {
  x: number;
  y: number;
  color: string;
  shade: number;
  warmth: number;
}
```

<!-- Config from oracle/src/config.ts -->
```typescript
export interface OracleConfig {
  solanaRpcUrl: string;
  oracleKeypair: Keypair;
  anthropicApiKey: string;
  programId: string;
  treasuryWallet: string;
  jackpotWallet: string;
  currentSeason: number;
  seasonStyleSummary: string;  // empty string for Season 1
  telegramBotToken?: string;
  telegramChatId?: string;
}
```

<!-- SPEC system prompt template (from SPEC.md lines 169-188) -->
Template with placeholders: {grid_width}, {grid_height}, {season_style_summary}
Response format: COLOR: / SHADE: / WARMTH: / REASONING:

<!-- SPEC per-round user message format (from SPEC.md lines 194-211) -->
Includes: canvas state, current pixel, neighbors, last 5 selections
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Build prompt construction module with unit tests</name>
  <files>
    oracle/src/prompt.ts,
    oracle/src/__tests__/prompt.test.ts
  </files>
  <behavior>
    - Test 1: buildSystemPrompt with empty season style summary produces prompt without style section
    - Test 2: buildSystemPrompt with "oceanic with volcanic interruptions" style summary includes it in the prompt
    - Test 3: buildUserMessage with empty canvas (pixel 0) produces "No pixels filled yet" and "No previous selections"
    - Test 4: buildUserMessage with 3 filled pixels includes all in canvas state section
    - Test 5: buildUserMessage correctly identifies neighbors (adjacent pixels) for a center pixel
    - Test 6: buildUserMessage includes last 5 round history entries with reasoning
    - Test 7: buildUserMessage with only 2 history entries shows just those 2 (no padding)
    - Test 8: hashPrompt produces consistent 32-byte SHA-256 hash for same input
    - Test 9: hashPrompt produces different hashes for different inputs
    - Test 10: buildFullPrompt concatenates system + user with double newline separator (for stable hashing)
    - Test 11: buildSystemPrompt injects season style summary when SEASON_STYLE_SUMMARY is non-empty (ORC-11)
  </behavior>
  <action>
    Create `oracle/src/prompt.ts`:
    - Export `buildSystemPrompt(gridWidth: number, gridHeight: number, seasonStyleSummary: string): string`
      - Load template from `config/system-prompt.txt` via `fs.readFileSync` (cache after first read)
      - Replace `{grid_width}` with gridWidth, `{grid_height}` with gridHeight
      - Replace `{season_style_summary}` with seasonStyleSummary (empty string for Season 1 — the placeholder line just becomes empty)
      - Return the complete system prompt string
    - Export `buildUserMessage(canvasPixels: PixelData[], currentX: number, currentY: number, gridWidth: number, gridHeight: number, history: RoundHistoryEntry[]): string`
      - Build "Current canvas state" section: list all filled pixels as "(x,y): Color, shade N, warmth N"
      - If no pixels filled: "No pixels filled yet."
      - Build "Current pixel to paint: (x, y)" line
      - Build "Neighboring pixels" section: for each adjacent position (up/down/left/right/diagonals), check if a filled pixel exists at that coordinate; list filled neighbors or "empty" for each
      - Build "Your last 5 selections" section: list up to 5 history entries as "N. (x,y): Color, shade N, warmth N - \"reasoning\""
      - If no history: "No previous selections."
      - End with "Select the color, shade, and warmth for pixel (x, y)."
    - Export `buildFullPrompt(systemPrompt: string, userMessage: string): string`
      - Concatenate: `systemPrompt + "\n\n" + userMessage`
      - This is what gets hashed — the concatenation must be deterministic
    - Export `hashPrompt(fullPrompt: string): Buffer`
      - `createHash('sha256').update(Buffer.from(fullPrompt, 'utf8')).digest()`
      - Return 32-byte Buffer

    IMPORTANT: Never normalize or trim the prompt after construction. The exact string that is hashed must be stored and uploaded to Arweave verbatim. Line endings must be `\n` (not `\r\n`). Use `\n` explicitly in template interpolation.

    Create `oracle/src/__tests__/prompt.test.ts`:
    - Implement all 11 test behaviors above
    - For neighbor tests: create a mock canvas with known pixel positions and verify correct neighbor detection
    - For hash tests: assert Buffer length is 32, assert determinism, assert different inputs produce different hashes
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/oracle && npx ts-mocha -p ./tsconfig.json -t 10000 src/__tests__/prompt.test.ts</automated>
  </verify>
  <done>prompt.ts exports buildSystemPrompt, buildUserMessage, buildFullPrompt, hashPrompt; all 11 prompt tests pass; hash output is consistent 32-byte SHA-256</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: Build Claude API client with response parsing, validation, and unit tests</name>
  <files>
    oracle/src/claude.ts,
    oracle/src/__tests__/claude.test.ts
  </files>
  <behavior>
    - Test 1: parseClaudeResponse extracts COLOR, SHADE, WARMTH, REASONING from valid response
    - Test 2: parseClaudeResponse handles response with preamble text before structured fields
    - Test 3: parseClaudeResponse returns null for response missing COLOR field
    - Test 4: parseClaudeResponse returns null for response missing REASONING field
    - Test 5: validateClaudeResult rejects colorIndex outside 0-15
    - Test 6: validateClaudeResult rejects shade outside 0-100
    - Test 7: validateClaudeResult rejects warmth outside 0-100
    - Test 8: validateClaudeResult accepts valid result { colorIndex: 7, shade: 50, warmth: 50 }
    - Test 9: parseClaudeResponse handles multi-line REASONING (takes only first line after REASONING:)
    - Test 10: parseClaudeResponse maps color name to correct index (Blue -> 7, Red -> 0, White -> 15)
    - Test 11: parseClaudeResponse returns null for unrecognized color name (e.g., "Turquoise")
  </behavior>
  <action>
    Create `oracle/src/claude.ts`:
    - Export `parseClaudeResponse(raw: string): ClaudeResult | null`
      - Use line-by-line regex matching (not positional parsing) per RESEARCH pitfall #3
      - Match: `/^COLOR:\s*(.+)$/m`, `/^SHADE:\s*(\d+)$/m`, `/^WARMTH:\s*(\d+)$/m`, `/^REASONING:\s*(.+)$/m`
      - Look up color name in COLOR_NAMES array (case-sensitive match after trim)
      - If any field missing or color not found, return null
      - Parse shade and warmth as integers
      - Return ClaudeResult with colorIndex, colorName, shade, warmth, reasoning

    - Export `validateClaudeResult(result: ClaudeResult): boolean`
      - Validate: colorIndex 0-15, shade 0-100, warmth 0-100
      - Return true if all valid, false otherwise

    - Export `callClaude(anthropicApiKey: string, systemPrompt: string, userMessage: string): Promise<ClaudeResult>`
      - Create Anthropic client with apiKey
      - Call `client.messages.create`:
        - model: 'claude-sonnet-4-6' (per CONTEXT.md locked decision — Claude Sonnet, not Haiku)
        - max_tokens: 256
        - temperature: 0 (NON-NEGOTIABLE)
        - system: systemPrompt
        - messages: [{ role: 'user', content: userMessage }]
      - Extract text from response.content[0]
      - Parse with parseClaudeResponse
      - Validate with validateClaudeResult
      - If parse fails or validation fails, throw Error with descriptive message including the raw response text (for debugging)
      - Return validated ClaudeResult
      - Log the call with logger: { event: 'claude_call', model, tokens_used: response.usage }

    Create `oracle/src/__tests__/claude.test.ts`:
    - Implement all 11 test behaviors above
    - Tests for parseClaudeResponse and validateClaudeResult are pure unit tests (no API mocking needed)
    - Do NOT test callClaude directly in unit tests (requires API key) — that is an integration test concern
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/oracle && npx ts-mocha -p ./tsconfig.json -t 10000 src/__tests__/claude.test.ts</automated>
  </verify>
  <done>claude.ts exports parseClaudeResponse, validateClaudeResult, callClaude; all 11 parsing/validation tests pass; callClaude uses claude-sonnet-4-6 at temperature 0</done>
</task>

</tasks>

<verification>
- `cd oracle && npx ts-mocha -p ./tsconfig.json -t 10000 src/__tests__/prompt.test.ts src/__tests__/claude.test.ts` — all 22 tests pass
- `cd oracle && npx tsc --noEmit` — no type errors
- prompt.ts hash output matches: `createHash('sha256').update(Buffer.from(text, 'utf8')).digest()` for known inputs
- claude.ts parseClaudeResponse handles preamble text, missing fields, invalid colors
</verification>

<success_criteria>
Prompt construction produces SPEC-compliant prompts with stable SHA-256 hashes. Claude response parsing handles all edge cases (preamble, missing fields, invalid values) with clear error reporting. Both modules are thoroughly unit-tested.
</success_criteria>

<output>
After completion, create `.planning/phases/02-oracle-service/02-03-SUMMARY.md`
</output>
