---
phase: 04-trust-layer-and-docs
plan: "01"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/types/game.ts
  - app/src/hooks/useSeasonData.ts
  - app/src/components/canvas/PixelCell.tsx
  - app/src/components/canvas/PixelTooltip.tsx
  - app/src/__tests__/PixelTooltip.test.tsx
autonomous: true
requirements: [FS-07, FS-08, FS-09]

must_haves:
  truths:
    - "VRF-resolved pixels display a visible colored dot in the corner of their cell on the canvas"
    - "Tapping a resolved pixel shows color name, shade, warmth, round number, and a 'Verified fair' badge"
    - "Tapping a resolved pixel and expanding the proof section shows the prompt hash hex, Arweave link, and VRF note if applicable"
  artifacts:
    - path: "app/src/components/canvas/PixelCell.tsx"
      provides: "VRF dot overlay on resolved VRF pixels"
      contains: "vrfResolved"
    - path: "app/src/components/canvas/PixelTooltip.tsx"
      provides: "Expandable proof section with commit-reveal data"
      contains: "promptHashHex"
    - path: "app/src/types/game.ts"
      provides: "Extended PixelSnapshot with promptHash and arweaveTxid fields"
      contains: "promptHash"
  key_links:
    - from: "app/src/hooks/useSeasonData.ts"
      to: "app/src/types/game.ts"
      via: "decodes promptHash/arweaveTxid from on-chain data into PixelSnapshot"
      pattern: "promptHash.*arweaveTxid"
    - from: "app/src/components/canvas/PixelTooltip.tsx"
      to: "PixelSnapshot"
      via: "reads promptHash and arweaveTxid from data prop"
      pattern: "data\\.promptHash"
---

<objective>
Extend pixel display with VRF visual distinction and commit-reveal proof verification.

Purpose: Players need to see which pixels were resolved via VRF fallback (visual trust signal on canvas) and verify that the AI's input was committed before they bet (fairness transparency). This is the core trust mechanism for the product.
Output: VRF dot on canvas pixels, extended PixelTooltip with "Verified fair" badge and expandable proof section showing prompt hash, Arweave link, and VRF explanation.
</objective>

<execution_context>
@/Users/puranjaysingh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/puranjaysingh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trust-layer-and-docs/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/src/types/game.ts:
```typescript
export interface PixelSnapshot {
  pixelIndex: number;
  colorIndex: number;
  shade: number;
  warmth: number;
  status: "open" | "locked" | "resolved";
  colorPools: number[];
  totalPool: number;
  openedAtSeconds: number | null;
  winningColor: number | null;
  vrfResolved: boolean;
}
```

From app/src/components/canvas/PixelCell.tsx:
```typescript
interface PixelCellProps {
  pixelIndex: number;
  isActive: boolean;
  data: PixelSnapshot | undefined;
  onTap: (pixelIndex: number) => void;
}
```

From app/src/components/canvas/PixelTooltip.tsx:
```typescript
interface PixelTooltipProps {
  pixelIndex: number;
  data: PixelSnapshot;
  position: { x: number; y: number };
  onDismiss: () => void;
}
// TOOLTIP_WIDTH = 160, TOOLTIP_HEIGHT = 120
```

From app/src/hooks/useSeasonData.ts:
```typescript
function decodePixelState(decoded: Record<string, unknown>, pixelIndex: number): PixelSnapshot
// Currently decodes: status, colorPools, totalPool, shade, warmth, winningColor, openedAtSeconds, vrfResolved
```

From app/src/lib/pda.ts:
```typescript
export function derivePixelPDA(programId: PublicKey, seasonNumber: number, pixelIndex: number): [PublicKey, number];
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Extend PixelSnapshot type and decode prompt hash + Arweave txid from on-chain data</name>
  <files>app/src/types/game.ts, app/src/hooks/useSeasonData.ts</files>
  <action>
1. In `app/src/types/game.ts`, add three fields to the `PixelSnapshot` interface:
   - `promptHash: number[] | null` — `[u8; 32]` decoded as number array, null for unresolved pixels
   - `arweaveTxid: string | null` — the 43-char Arweave transaction ID string, null if `has_arweave_txid` is false
   - `hasArweaveTxid: boolean` — mirrors on-chain `has_arweave_txid` flag

2. In `app/src/hooks/useSeasonData.ts`, update the `decodePixelState` function to extract the new fields:
   - `promptHash`: Read `decoded.promptHash` as `number[]`. This is `[u8; 32]` from the on-chain PixelState.
   - `hasArweaveTxid`: Read `decoded.hasArweaveTxid` as boolean.
   - `arweaveTxid`: If `hasArweaveTxid` is true, read `decoded.arweaveTxid` as `number[]` (the `[u8; 43]` bytes), then convert to string via `String.fromCharCode(...bytes)`. If false, set to null.
   - For unresolved pixels (status !== "resolved"), set `promptHash: null`, `arweaveTxid: null`, `hasArweaveTxid: false`.

IMPORTANT: The `arweave_txid` on-chain field stores 43 ASCII character codes representing the base64url Arweave transaction ID. Decode with `String.fromCharCode(...)`, NOT base64 encoding.

Also update the `decodePixelStateToSnapshot` function in `app/src/hooks/useHeliusSocket.ts` — it duplicates `decodePixelState` logic and must be kept in sync with the same three new fields.
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npx tsc --noEmit 2>&1 | head -20</automated>
  </verify>
  <done>PixelSnapshot interface includes promptHash, arweaveTxid, hasArweaveTxid. Both decode functions (useSeasonData and useHeliusSocket) extract and convert these fields from on-chain data. TypeScript compiles without errors.</done>
</task>

<task type="auto">
  <name>Task 2: Add VRF dot to PixelCell and expandable proof section to PixelTooltip</name>
  <files>app/src/components/canvas/PixelCell.tsx, app/src/components/canvas/PixelTooltip.tsx, app/src/__tests__/PixelTooltip.test.tsx</files>
  <action>
1. **PixelCell VRF dot (FS-07):**
   In `PixelCell.tsx`, when `data?.vrfResolved === true` AND `data?.status === "resolved"`, render a small colored dot in the top-right corner of the pixel cell. The dot should be:
   - 6px diameter circle
   - Splat Cyan color (#3BDBFF)
   - Positioned absolute, top: 2px, right: 2px
   - The parent div already has no `position: relative`, so add `position: "relative"` to the cell style when resolved to make absolute positioning work.

2. **PixelTooltip proof section (FS-08, FS-09):**
   Extend `PixelTooltip.tsx` with the following changes:

   a. Remove the hardcoded `TOOLTIP_HEIGHT = 120` constant. Instead, let the tooltip auto-size based on content. Keep `TOOLTIP_WIDTH = 200` (increase from 160 to accommodate proof data). Update `clampToViewport` to accept a dynamic height parameter — use `tooltipRef.current?.getBoundingClientRect().height ?? 160` after render via a useEffect re-clamp.

   b. For resolved pixels, add a "Verified fair" badge below the existing shade/warmth/round display:
      - Green checkmark icon (unicode ✓) + "Verified fair" text in Splat Cyan (#3BDBFF)
      - If `data.vrfResolved` is true, show "Resolved via random fallback" instead in a muted orange (#ED8936)

   c. Add an expandable "View proof" link below the badge. When clicked, it toggles an expanded section showing:
      - **Prompt hash:** Convert `data.promptHash` (number[]) to hex string: `data.promptHash.map(b => b.toString(16).padStart(2, '0')).join('')`. Display truncated (first 8 + last 8 chars with "...") in mono font (JetBrains Mono).
      - **Arweave link:** If `data.arweaveTxid` is non-null, show "View prompt on Arweave" as a link to `https://arweave.net/${data.arweaveTxid}`. Open in new tab.
      - **VRF note:** If `data.vrfResolved`, show "This pixel was resolved using Switchboard VRF (verifiable random function) because the AI was unavailable."

   d. The expanded state is controlled by local `useState<boolean>(false)`. "View proof" toggles it. The expand/collapse uses a simple height transition.

   e. Use "prompt commitment proof" language per user decision — never "reproducible result".

3. **Tests (FS-08, FS-09):**
   Create `app/src/__tests__/PixelTooltip.test.tsx`:
   - Test: resolved pixel shows color name, shade, warmth, and "Round N" text
   - Test: resolved pixel shows "Verified fair" badge
   - Test: VRF-resolved pixel shows "random fallback" text instead
   - Test: clicking "View proof" expands to show prompt hash hex
   - Test: Arweave link renders with correct href when arweaveTxid is non-null
   - Test: Arweave link is absent when arweaveTxid is null
   - Use `@testing-library/react` render + screen queries. Mock PixelSnapshot data with known promptHash bytes and arweaveTxid string.
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm test -- --reporter=verbose PixelTooltip 2>&1 | tail -30</automated>
  </verify>
  <done>VRF-resolved pixels show a cyan dot in the corner on canvas. PixelTooltip displays "Verified fair" badge (or VRF fallback note), expandable proof section with prompt hash hex and Arweave link. All PixelTooltip tests pass.</done>
</task>

</tasks>

<verification>
- `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npx tsc --noEmit` — zero TypeScript errors
- `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm test -- --reporter=verbose` — all tests pass
- `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm run build` — production build succeeds
</verification>

<success_criteria>
- FS-07: VRF-resolved pixels have a visible cyan dot in the top-right corner of their cell
- FS-08: Tapping a resolved pixel shows color, shade, warmth, and round number
- FS-09: "Verified fair" badge visible; expandable proof section shows prompt hash hex, Arweave link (when available), and VRF explanation (when applicable)
- All copy uses "prompt commitment proof" language, never "reproducible result"
</success_criteria>

<output>
After completion, create `.planning/phases/04-trust-layer-and-docs/04-01-SUMMARY.md`
</output>
