---
phase: 04-trust-layer-and-docs
plan: "03"
type: execute
wave: 1
depends_on: []
files_modified:
  - app/src/hooks/useSeasonCompletion.ts
  - app/src/components/season/SeasonCompleteOverlay.tsx
  - app/src/components/season/IntermissionScreen.tsx
  - app/src/components/share/ShareCanvas.tsx
  - app/src/__tests__/SeasonCompleteOverlay.test.tsx
  - app/package.json
autonomous: true
requirements: [FS-04, FS-05, FS-06]

must_haves:
  truths:
    - "Player sees a full-screen celebration overlay when the final pixel resolves (season completion)"
    - "Player can generate and download a branded PNG of the completed canvas with season title and player stats"
    - "Player sees a 12-hour intermission countdown with season stats after season completion"
  artifacts:
    - path: "app/src/hooks/useSeasonCompletion.ts"
      provides: "Season status detection from on-chain SeasonState"
      exports: ["useSeasonCompletion"]
    - path: "app/src/components/season/SeasonCompleteOverlay.tsx"
      provides: "Full-screen takeover with confetti, canvas zoom, and season stats"
      exports: ["SeasonCompleteOverlay"]
    - path: "app/src/components/season/IntermissionScreen.tsx"
      provides: "12-hour countdown with completed canvas and season stats"
      exports: ["IntermissionScreen"]
    - path: "app/src/components/share/ShareCanvas.tsx"
      provides: "Branded PNG generation and share/download"
      exports: ["ShareCanvas"]
  key_links:
    - from: "app/src/hooks/useSeasonCompletion.ts"
      to: "SeasonState on-chain"
      via: "decodes SeasonState.status enum and completed_at timestamp"
      pattern: "completedAt|seasonStatus"
    - from: "app/src/components/share/ShareCanvas.tsx"
      to: "html-to-image"
      via: "toPng() on offscreen branded div"
      pattern: "toPng"
---

<objective>
Build season completion celebration, intermission countdown, and branded PNG share feature.

Purpose: Season completion is the "you beat the game" moment. The full-screen takeover, confetti, and share PNG create the reward loop that drives player retention and social sharing. The intermission screen keeps players engaged between seasons.
Output: SeasonCompleteOverlay with confetti and canvas zoom, IntermissionScreen with 12h countdown, ShareCanvas for branded PNG generation.
</objective>

<execution_context>
@/Users/puranjaysingh/.claude/get-shit-done/workflows/execute-plan.md
@/Users/puranjaysingh/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-trust-layer-and-docs/04-RESEARCH.md

<interfaces>
<!-- Key types and contracts the executor needs. Extracted from codebase. -->

From app/src/hooks/useResolution.ts:
```typescript
// Pattern for status transition detection:
const prevStatusRef = useRef<string | null>(null);
// Detect: currentStatus === "completed" && prevStatus !== "completed"
```

From app/src/hooks/useSeasonData.ts:
```typescript
// SeasonState decode pattern:
const decodedSeason = program.coder.accounts.decode("SeasonState", seasonAccountInfo.data);
// Fields: currentPixelIndex, seasonNumber, status, completedAt
// Status enum: { active?: object, completed?: object, intermission?: object }
```

From app/src/components/ui/WinNotification.tsx:
```typescript
// Confetti pattern (already installed canvas-confetti):
import confetti from "canvas-confetti";
confetti({
  particleCount: 3, angle: 60, spread: 55, origin: { x: 0 },
  colors: ["#FF3B6F", "#FF6B3B", "#3BDBFF", "#FFDD3B", "#A855F7", "#22D3EE"],
});
```

From app/src/store/gameStore.ts:
```typescript
export const useGameStore = create<GameState>()((set) => ({
  seasonNumber: 0,
  currentPixelIndex: 0,
  pixels: Record<number, PixelSnapshot>,
  // ...
}));
```

From app/src/lib/color.ts:
```typescript
export const COLOR_NAMES: string[];
export const BASE_HEX: Record<string, string>;
export function computePixelColor(colorIndex: number, shade: number, warmth: number): string;
```

From app/src/lib/constants.ts:
```typescript
export const GRID_SIZE = 10;
export const TOTAL_PIXELS = 100;
```
</interfaces>
</context>

<tasks>

<task type="auto">
  <name>Task 1: Build useSeasonCompletion hook and SeasonCompleteOverlay</name>
  <files>app/src/hooks/useSeasonCompletion.ts, app/src/components/season/SeasonCompleteOverlay.tsx, app/src/__tests__/SeasonCompleteOverlay.test.tsx</files>
  <action>
1. **useSeasonCompletion hook** (`app/src/hooks/useSeasonCompletion.ts`):
   - Export interface `SeasonCompletionState`:
     ```
     seasonStatus: "active" | "completed" | "intermission"
     completedAtSeconds: number | null    // Unix timestamp when season completed
     intermissionEndsSeconds: number | null  // completedAt + 12 * 3600
     seasonNumber: number
     ```
   - Hook reads from the Zustand store and connection. On mount and on Helius WebSocket season account updates (which already trigger `setSeasonState`), decode the SeasonState's `status` field:
     ```
     const statusRaw = decoded.status as { active?: object; completed?: object; intermission?: object };
     ```
   - Extract `completedAt` as `decoded.completedAt` (camelCase — Anchor decoder pattern). If completed or intermission, compute `intermissionEndsSeconds = Number(completedAt) + 12 * 3600`.
   - NOTE: This hook needs to fetch the SeasonState account directly (like useSeasonData does) because the existing Zustand store doesn't track season status or completedAt. Fetch on mount and provide a `refresh()` callback.
   - Returns `SeasonCompletionState` plus `loading` and `refresh`.

2. **SeasonCompleteOverlay** (`app/src/components/season/SeasonCompleteOverlay.tsx`):
   - Props: `{ seasonNumber: number, pixels: Record<number, PixelSnapshot>, onDismiss: () => void }`
   - Full-screen fixed overlay (z-index 600, above drawer):
     a. Dark semi-transparent backdrop fading in
     b. "Season {N} Complete!" heading in Luckiest Guy display font, white, centered
     c. 10x10 canvas grid rendered at larger size (zoomed) — use `computePixelColor` for each pixel to render color swatches. Each cell is a colored div. Grid is centered and scales to fill ~80% of viewport width (max 400px).
     d. Confetti burst on mount — use canvas-confetti with sustained burst pattern (4 seconds, rAF loop like WinNotification but longer). 6 brand colors: #FF3B6F, #FF6B3B, #3BDBFF, #FFDD3B, #A855F7, #22D3EE.
     e. Season stats below canvas: "100 rounds", total pool (sum all pixel totalPool values), displayed in Nunito.
     f. "Share Canvas" button linking to ShareCanvas component (triggers PNG generation).
     g. "Continue" button to dismiss the overlay.
   - All animations respect `prefers-reduced-motion` — skip confetti if reduced motion, instant transitions.
   - Motion: `initial={{ opacity: 0 }} animate={{ opacity: 1 }} transition={{ duration: 0.5 }}`

3. **Tests** (`app/src/__tests__/SeasonCompleteOverlay.test.tsx`):
   - Test: overlay renders "Season N Complete!" heading
   - Test: 100 pixel cells rendered in the canvas grid
   - Test: "Share Canvas" button is present
   - Test: "Continue" button calls onDismiss when clicked
   - Mock pixels as Record<number, PixelSnapshot> with 100 entries (resolved status, various colors).
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm test -- --reporter=verbose SeasonCompleteOverlay 2>&1 | tail -20</automated>
  </verify>
  <done>useSeasonCompletion detects season completed/intermission status from on-chain data. SeasonCompleteOverlay renders full-screen celebration with confetti, zoomed canvas grid, season stats, and share button. All tests pass.</done>
</task>

<task type="auto">
  <name>Task 2: Build IntermissionScreen and ShareCanvas with PNG generation</name>
  <files>app/src/components/season/IntermissionScreen.tsx, app/src/components/share/ShareCanvas.tsx, app/src/__tests__/IntermissionScreen.test.tsx, app/package.json</files>
  <action>
1. **Install html-to-image:**
   Run `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm install html-to-image`

2. **ShareCanvas** (`app/src/components/share/ShareCanvas.tsx`):
   - "use client" directive required (DOM APIs).
   - Props: `{ seasonNumber: number, pixels: Record<number, PixelSnapshot>, playerStats: { totalBets: number, correctPredictions: number, hitRate: string } | null }`
   - Renders a hidden offscreen `<div ref={shareRef}>` with branded layout:
     a. Dark border frame (#14141F) with 24px padding
     b. "Pixel Predict -- Season {N}" heading at top in white, Luckiest Guy font
     c. 10x10 canvas grid centered — each cell rendered as a colored div using `computePixelColor`. Use INLINE styles with resolved hex values (not CSS variables) because html-to-image may not resolve CSS custom properties in offscreen elements (Pitfall 2 from research).
     d. Player stats at bottom: "Bets: {N} | Wins: {N} | Hit Rate: {N}%" — only if playerStats is provided
     e. "pixelpredict.com" URL at bottom in muted gray
   - The offscreen element: `position: "absolute", left: "-9999px", visibility: "hidden"` but still in DOM.
   - Export `useShareCanvas()` hook returning `{ shareRef: RefObject, handleShare: () => Promise<void>, isGenerating: boolean }`:
     a. `handleShare`: calls `toPng(shareRef.current, { pixelRatio: 2 })` from html-to-image
     b. Then checks `navigator.canShare?.({ files: [file] })` — if supported, use `navigator.share`
     c. Fallback: programmatic `<a download>` click to download "splat-season-{N}.png"
     d. Sets `isGenerating` during the async operation for loading state
   - Also export `ShareCanvasTemplate` component (the offscreen div) for embedding in parent components.

3. **IntermissionScreen** (`app/src/components/season/IntermissionScreen.tsx`):
   - Props: `{ seasonNumber: number, pixels: Record<number, PixelSnapshot>, intermissionEndsSeconds: number, playerStats: { totalBets: number, correctPredictions: number, hitRate: string } | null }`
   - Full-page content (replaces game content during intermission, not an overlay):
     a. Big countdown timer to next season: compute remaining seconds from `intermissionEndsSeconds - Math.floor(Date.now() / 1000)`. Display as HH:MM:SS. Update every second via `setInterval`. Use Luckiest Guy font, large size (2.5rem), white.
     b. "Next Season starts in..." label above the countdown in muted gray.
     c. Completed canvas displayed at medium size (similar to SeasonCompleteOverlay grid).
     d. Season stats summary below canvas: "100 rounds played", total pool across all pixels.
     e. "Share Canvas" button wired to ShareCanvas component.
     f. "View Canvas" button (scrolls to the canvas grid on this page — simple anchor scroll).
   - When countdown reaches 0, display "New season starting..." and the parent should handle navigation/refresh.
   - ShareCanvasTemplate embedded as offscreen sibling.

4. **Tests** (`app/src/__tests__/IntermissionScreen.test.tsx`):
   - Test: renders "Next Season starts in..." heading
   - Test: displays countdown in HH:MM:SS format
   - Test: "Share Canvas" button is present
   - Test: 100 pixel cells rendered in the canvas display
   - Mock Date.now() to control countdown math.
  </action>
  <verify>
    <automated>cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm test -- --reporter=verbose IntermissionScreen 2>&1 | tail -20</automated>
  </verify>
  <done>html-to-image installed. ShareCanvas generates branded PNG with dark frame, canvas grid, stats, and URL — supports navigator.share and download fallback. IntermissionScreen shows countdown timer, completed canvas, and share button. All tests pass.</done>
</task>

</tasks>

<verification>
- `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npx tsc --noEmit` — zero TypeScript errors
- `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm test -- --reporter=verbose` — all tests pass
- `cd /Users/puranjaysingh/Documents/Claude2026/splat/app && npm run build` — production build succeeds
- html-to-image appears in app/package.json dependencies
</verification>

<success_criteria>
- FS-04: Full-screen celebration overlay with confetti and zoomed canvas grid appears when season completes
- FS-05: "Share Canvas" button generates branded PNG with dark frame, "Pixel Predict -- Season X" title, 10x10 canvas, player stats, and site URL; supports mobile share and desktop download
- FS-06: 12-hour intermission screen with live countdown (HH:MM:SS), completed canvas display, season stats, and share button
</success_criteria>

<output>
After completion, create `.planning/phases/04-trust-layer-and-docs/04-03-SUMMARY.md`
</output>
